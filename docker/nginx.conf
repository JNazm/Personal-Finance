user www-data;
worker_processes auto;
error_log /dev/stderr warn;
pid /tmp/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    access_log /dev/stdout;
    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 20M;

    server {
        listen 80;
        server_name _;
        root /var/www/html/public;
        index index.php index.html;

        charset utf-8;

        # Cloudflare Tunnel routes the full "/personal-finance/..." path through to
        # this container unchanged (unlike the Caddy setup in docker/Caddyfile, which
        # strips the prefix before proxying here). Strip it internally too, so this
        # nginx config works correctly whichever front door is used.
        rewrite ^/personal-finance$ /personal-finance/ permanent;
        rewrite ^/personal-finance/(.*)$ /$1 break;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location = /favicon.ico { access_log off; log_not_found off; }
        location = /robots.txt  { access_log off; log_not_found off; }

        error_page 404 /index.php;

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
            include fastcgi_params;
            fastcgi_read_timeout 300;
            # $request_uri (used by fastcgi_params' default REQUEST_URI) still
            # reflects the client's original, unrewritten URI, but Laravel's
            # router reads REQUEST_URI to determine the request path. Override
            # it to the internally-rewritten $uri so Laravel sees the stripped
            # path (without the /personal-finance prefix) in both scenarios.
            fastcgi_param REQUEST_URI $uri$is_args$args;
        }

        location ~ /\.(?!well-known).* {
            deny all;
        }
    }
}
